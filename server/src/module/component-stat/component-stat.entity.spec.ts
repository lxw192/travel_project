import { ComponentStatEntity } from './component-stat.entity';

describe('ComponentStatEntity', () => {
  it('should be defined', () => {
    expect(new ComponentStatEntity()).toBeDefined();
  });
});
