import { ComponentPropEntity } from './component-prop.entity';

describe('ComponentPropEntity', () => {
  it('should be defined', () => {
    expect(new ComponentPropEntity()).toBeDefined();
  });
});
